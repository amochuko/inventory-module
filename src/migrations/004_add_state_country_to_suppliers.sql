ALTER TABLE inventory.suppliers
ADD COLUMN state VARCHAR(100),
ADD COLUMN country VARCHAR(100); 
-- TODO: Add a table to poulate countries and states and add foreign key constraints to these columns   

